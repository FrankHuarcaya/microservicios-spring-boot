spring:
  application:
    name: microservices-gateway

  cloud:
    config:
      uri: http://config-server:8888 # Dirección del Config Server
      fail-fast: true # Falla si no puede conectarse al Config Server