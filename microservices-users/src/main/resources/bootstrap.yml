spring:
  application:
    name: microservices-users
  cloud:
    config:
      uri: http://config-server:8888 # Dirección del Config Server
      fail-fast: true

logging:
  level:
    org.springframework.cloud: DEBUG
    org.springframework.web: DEBUG