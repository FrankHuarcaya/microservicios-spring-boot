spring:
  application:
    name: microservices-users
  cloud:
    config:
      #uri: http://config-server:8888 # Dirección del Config Server en docker
      uri: http://localhost:8888 # LocalHost
      fail-fast: true

logging:
  level:
    org.springframework.cloud: DEBUG
    org.springframework.web: DEBUG