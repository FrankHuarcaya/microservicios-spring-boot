spring:
  application:
    name: microservices-cursos # Nombre del microservicio
  cloud:
    config:
      uri: http://config-server:8888 # Dirección del Config Server

      fail-fast: true # Si el Config Server no está disponible, falla rápido
