spring:
  application:
    name: microservices-exams
  cloud:
    config:
      #uri: http://config-server:8888 # Dirección del Config Server
      uri: http://localhost:8888 # Dirección del Config Server

      fail-fast: true